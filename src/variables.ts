const variables = {
  // Temporary solution - process.env is throwing an error durring production build
  apiBaseUri: 'https://api.eduadministrator.com',
  appName: 'Eduadministrator'
};

export default variables;
